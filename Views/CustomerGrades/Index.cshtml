@model IEnumerable<TermProject_S1.Models.CustomerGrade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerSalesLevel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerSalesLevel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomerGradeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerGradeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerGradeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
